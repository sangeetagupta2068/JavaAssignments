package com.domain.sangeetagupta.multi_threading_demo;

public class MyThread {
    //what happens if we don't call start method and then call run, sequential running of run will happen. Object won't behave like a thread
    //if start and run called together, no exception, thread behaves like thread and run will work like a normal object method.
}
